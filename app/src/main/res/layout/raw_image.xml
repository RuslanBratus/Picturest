<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:gravity="center">

    <ImageView
        android:id="@+id/rawImage"
        android:layout_width="110dp"
        android:layout_height="110dp"
        android:src="@drawable/ic_launcher_background"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/rawImageText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/gray_text"
        android:textSize="16sp"
        android:layout_marginTop="1dp"
        app:layout_constraintStart_toStartOf="@id/rawImage"
        app:layout_constraintEnd_toEndOf="@id/rawImage"
        app:layout_constraintTop_toBottomOf="@id/rawImage"/>

    <CheckBox
        android:id="@+id/rawImageCheckBox"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:checked="true"
        android:enabled="false"
        android:visibility="invisible"
        android:button="@drawable/check_box_stater"
        app:layout_constraintTop_toTopOf="@id/rawImage"
        app:layout_constraintEnd_toEndOf="@id/rawImage"
        android:layout_marginEnd="5dp"
        android:layout_marginTop="5dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>